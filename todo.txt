* Reduce moving things from one register to another unnecessarily
* Set members by name in struct literals
* Verify small structs are passed properly through parameters
* Some allocator that gets reset after each stage.
* Operator overloading?
* Struct literals for static variable initial value.
* Rename struct literals to group literals? They can also be arrays.
* Know why a job yielded, don't brute force until works, and detect when compilation would never
end.
* Add TEST instructions
* Avoid duplicate string literals
