#linklib "output/out.a";

LibAddTwoNumbers :: #external (a : s64, b : s64) -> s64;

Main :: () -> s64
{
	sum := LibAddTwoNumbers(1, 2);
	PrintF("1 + 2 is %, says statically linked library.\n", sum);
	if sum != 3 return 1;

	Print("Test ok\n");
	return 0;
}
