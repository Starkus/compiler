arrayAsArgument :: (array : 5 s16)
{
	foo := array[3];
}

main :: () -> s64
{
	arrayA : 20 u8;
	arrayB : 32 s32;

	foo : u8 = arrayA[2];
	bar := arrayB[16];

	arrayC : 5 s16;
	arrayAsArgument(arrayC);

	return 0;
}
