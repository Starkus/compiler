#include "core/basic.emi";
#include "core/print.emi";

MyType :: #alias s16;
PointerType :: #alias ^s64;
Nested :: #alias ^MyType;
ProcType :: #alias (s32, u64, bool) -> f32;

AProc :: (a : s32, b : u64, bool) -> f32
{
	Print("Hi\n");
	return 5.9879;
}

Main :: (args : [] String) -> s64
{
	anS16 : MyType = 5;
	ptr : PointerType;
	foo : Nested = ^anS16;

	aProcPointer : ProcType = AProc;
	anF32 : f32 = aProcPointer(5, 6, true);

	{
		ScopedType :: #type ^^MyType;
	}
	//outOfScope : ScopedType;

	sizeOfS32 := sizeof(s32);
	PrintF("Size of s32 is %\n", sizeOfS32);

	Print("Test ok\n");
	return 0;
}
